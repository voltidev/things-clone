{{#app-main
  hasContent=this.hasContent
  folder="today"
  as |selectBetween|
}}
  {{items-list
    items=(sort-by "order" (filter-by-tags this.items))
    selectBetween=selectBetween
  }}
{{/app-main}}