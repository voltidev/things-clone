{{#if isEditing}}
  {{task-form
    task=this.taskEditor.task
    placeholder=this.placeholder
    stopEditing=(action "stopEditing")
  }}
{{else}}
  {{task-info
    task=@task
    placeholder=this.placeholder
    isProjectShown=this.isProjectShown
    toggleCheckbox=(action "toggleCheckbox")
  }}
{{/if}}
